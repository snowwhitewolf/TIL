#  python 기초

```
python 개발 환경

idle cmd 프롬포트 - 한줄씩 실행
jupyter - 브라우저에서 코드를 실행
```



   ```
   주석
   
   #
   여러줄의 주석은 docstring 을 위해 사용
   vs코드에서는 블럭 처리 후 컨트롤 + /
   코드는 한줄에 한문장이 원칙
   ```



```
변수

할당 연산자 
=를 통해 값을 할당

type()
변수에 할당된 값의 타입 확인

id()
변수의 메모리 주소를 확인

같은 값을 동시에 할당 가능 
ex) x = y =1004

다른 값을 동시에 할당 가능
ex)  x, y = 1, 2

값 swap
임시변수 활용
tmp = x
x = y
y = tmp

y, x = x, y
```

```
식별자

변수, 함수, 모듈, 클래스 등의 이름

규칙
알파벳, _, 숫자로 구성
첫글자에 숫자 불가능
길이제한 없고 대소문자 구분
다음의 키워드는 예약어로 사용할수 없음 ex) false none true and as
```

```
숫자
int 정수
float 실수
complex 복소수

문자열(string)

참거짓(boolean)

none
```

```
숫자
모든 정수의 타입은 int
매우 큰수를 나타낼 때 overflow가 발생하지 않음

실수는 지수 표기법 사용

floating point rounding error
부동소수점에서 실수 연산 과정에서 발생 가능
값 비교하는 과정에서 정수가 아닌 실수인 경우 주의할 것

3.14 – 3.02 == 0.12
false

3.14-3.02
0.1200000000001
이진법으로 표현하기 때문에 가장 가까운 근사치값을 넣어줌
isclose(a,b) 로 확인 가능

complex

허수부를 j로 표현
```

```
문자열

따옴표는 통일해서 사용
```

```
자리표시자

% formatting

str.format()

f-strings
```

```
참거짓
다음은 모두 false로 변환
0, 0.0, () [] {} ‘’ none

None 
값이 없음을 표현하기 위한 타입(N이 대문자)
```

```
타입 변환

암시적 타입변환
명시적 타입변환

int(‘3’)		가능
int(‘3.5’)		불가능
float(‘3/4’)	불가능
```

```
연산자
\  +  -  *  /  //  **  %

나눗셈은 항상 결과가 float
divmod() - 몫과 나머지가 한번에 나옴

비교연산자
!= 같지않음
is 메모리 주소 비교
```

```
복합연산자

a += b    a = a + b
```

```
+는 문자열간의 결합 가능 리스트도 가능

포함검사
‘a’ in ‘apple’
true 

identity
-5부터 256까지 숫자의id는 동일


인덱싱 슬라이싱
[]를 통해 값을 접근 [:]를 통해 슬라이싱 가능



표현식(식별자, 값, 연산자)
문장-실행가능한 최소한의 코드단위
모든 표현식은 문장(예외 존재)
```

```
컨테이너

시퀀스형(순서가 있다)(정렬되어있다 X)		리스트 튜플 레인지 문자형 바이너리

비시퀀스형(순서가 없다)				   세트 딕셔너리


리스트
[] list()
값에 대한 접근은 list[i]
서로 다른 타입의 데이터를 저장할수 있다
중첩도 가능


튜플
수정 불가능한 시쿼스임
(()) 혹은 tuple()로 생성
파이썬 내부에서 보통 사용
하나의 항목만 튜플로 만들때는 쉼표를 붙여야함 (1,)
```

```
레인지

range(n)
0부터 n-1까지

range(n,m)
n부터 m-1까지

range(n,m,s)
n부터 m-1까지 s만큼 더해서

담겨있는 숫자 확인위해 리스트로 형변환
실제 사용시에는 변환 필요 x
레인지는 결합 x 반복 x
```

```
인덱싱

[i] 리스트 튜플 레인지 문자열에 사용가능

슬라이싱
[1:3],[:2]

len() 시쿼스의 길이
min()
max()
.count() 특정 원소의 개수(없으면 0 반환)
```

 ```
 비시퀀스
 세트 딕셔너리
 
 세트
 
 {} 혹은 set()를 통해 생성
 {} 딕셔너리? 같음 딕셔너리는 키밸류 쌍으로 되있어서 안의 구조가 다름
 빈 중괄호는 딕셔너리
 반드시 set()로 생성 중괄호 x
 순서가 없어 별도의 값에 접근할수없음
 집합과 동일한 구조
 집합연산이 가능 중복된값이 존재하지 않음
 
 세트 활용
 세트를 활용하면 다른 컨테이너에서 중복된 값을 쉽게 제거할수 있음
 단 이후 순서가 무시되므로 순서가 중요한 경우 사용할수 없음
 ```

```
딕셔너리

key의 value가 쌍으로 이뤄진 자료구조
key는 변경 불가능한 데이터만 활용가능
value는 모든값으로 설정가능(리스트, 딕셔너리 등)
```

```
컨테이너 형변환


변경 불가능한 데이터(딕셔너리의 키로 활용가능)
리터럴-숫자, 문자열, 참거짓
range
tuple

변경불가능한 값의 복사

서로 달라질수 있음


변경 가능한 데이터의 복사(list set dictionary)

같은 값이 복사되어 항상 같음
```

```
조건문

if문은 조건식과 함께 사용 

if <expression>:
	#code
else:
	#code

인풋은 문자열이기 떄문에 int로 형변환 해야함

num = int(input(‘숫자입력: ’))

복수의 조건식은 elif

중첩 조건문
들여쓰기 조심

조건 표현식

True if 조건  else False

할당은 한번만

value = num if num>=0 else –num
```

````
반복문

while문
종료조건에 해당하는 코드를 통해 반복문을 종료시켜야함

for문
반복가능한(iterable) 객체를 모두 순회하면 종료(별도 종료조건 x)

반복제어
break, continue, for-else
```

```
while 

조건이 false가 될 때까지 반복
무한 루프 조심

while <expression>:
	#code block
```

```
for문

for문은 시퀀스(string, tuple, list, range)를 포함한 iterable한 객체 요소를 모두 순회함
처음부터 끝까지 모두 순회하므로 별도의 종료 조건이 필요하지 않음

for <변수명> in <iterable>:
	#code
chars = input()
for char in chars:
	print(char)


enumerate 
리스트를 순회하면서 index값을 같이 출력할수 있음
```

```
반복문 제어

break
반복문을 종료

continue
continue이후의 코드블록은 수행하지 않고 다음 반복을 수행

for-else
끝까지 반복문을 실행한 이후에 else문 실행

break를 통해 중간에 종료되는 경우 else문은 실행되지않음
```

 ```
 pass문
 
 아무것도 하지않음
 특별히 할 일이 없을 때 자리를 채우는 용도로 사용
 반복문이 아니어도 사용 가능
 ```

   

